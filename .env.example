# Solo necesario para ENVIAR el correo (npm run digest:send).
# Con Google Workspace: crea una "contraseña de aplicación" en
# https://myaccount.google.com/apppasswords y úsala como SMTP_PASS.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tu.correo@xertica.com
SMTP_PASS=xxxxxxxxxxxxxxxx
